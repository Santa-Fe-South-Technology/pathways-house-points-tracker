window.HOUSE_POINTS_CONFIG = {
    // Set this to your deployed backend URL, for example:
    // API_BASE_URL: 'https://pathways-house-points-api.onrender.com'
    //
    // Leave as location.origin for local development with `npm start`.
    API_BASE_URL: window.location.origin,
};
